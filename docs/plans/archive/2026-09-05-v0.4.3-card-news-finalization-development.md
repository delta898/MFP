# v0.4.3 Card News Finalization

## Branch

- Branch: `codex/feat/v0.4.3-card-news-finalization`
- Base/parent branch: `codex/feat/v0.4.3-card-news-main`
- Start date: 2026-09-05
- Status: complete; parent is ready for `dev` integration

## User Need

The manual Card News feature is complete enough for v0.4.3. Before integrating the parent into `dev`, completed development records, the parent plan, the actionable backlog, and regression evidence must reflect the actual product state.

## Goal

Close the manual Card News feature cleanly and leave the parent branch as a reviewed, documented `dev` merge candidate without performing the `dev` merge.

## Scope

- Finalize and archive completed Card News stage records still under `docs/plans/active/`.
- Update the parent development record with the final delivered scope, resolved decisions, remaining risks, and verification evidence.
- Remove completed Card News feature work from the backlog while preserving genuinely pending follow-ups such as optional Bitly URL shortening.
- Run final unit and browser regression checks for the parent feature.
- Commit this finalization stage, merge it into the Card News parent, and delete the sub-feature branch.

## Explicit Non-goals

- Automatic Card News creation or scheduled publication.
- Additional UI polish or optional feature expansion.
- Merging the Card News parent into `dev`.
- Version bump, release branch updates, tagging, push, or deployment.

## Proposed Design

Treat this as a documentation and verification closure stage. Completed stage records remain independently understandable in the archive. The parent record becomes the concise source of truth for what v0.4.3 delivers, while the backlog retains only actionable future work rather than duplicating release history.

## Verification Plan

- Documentation consistency and clean diff checks.
- Full unit suite.
- Fixture-only browser UI smoke test.
- Confirm clean working tree and parent branch after integration.

## Progress

- 2026-09-05: finalization branch created from the completed Card News parent.
- 2026-09-05: archived the remaining completed Card News stage records, promoted the delivered workflow to the canonical feature documentation, and refreshed the parent plan and actionable backlog.
- 2026-09-05: full unit regression passed (1444/1444). Fixture-only browser UI smoke regression passed (190 fixture requests).

## Final Result

The manual Card News feature is documented and verified as a `dev` merge candidate. No version change, release action, remote publish, or `dev` merge was performed in this stage. The user's earlier hands-on acceptance remains the visual and exploratory confirmation; this stage added only regression verification and documentation closure.
