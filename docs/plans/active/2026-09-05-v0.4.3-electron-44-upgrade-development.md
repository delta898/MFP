# v0.4.3 Electron 44 Upgrade Development Record

## Branch
- Branch: `codex/fix/v0.4.3-electron-44-upgrade`
- Base/parent branch: `release/v0.4.3`
- Start date: 2026-09-05
- Status: Electron upgrade and packaged first-run guidance regression complete for the v0.4.3-dev2 checkpoint

## User Need
A Windows user experienced an application window whose content did not appear. The exact cause could not be confirmed because follow-up logs and reproduction results were unavailable. Independently, the current Electron 40 runtime has reached end of support, so the user wants to upgrade Electron during v0.4.3 preparation and validate the application before considering GPU-specific changes.

## Goal
Upgrade the desktop runtime from Electron 40 to the current supported Electron 44 line while preserving existing behavior and validating startup, UI loading, packaging contracts, and native dependency boundaries.

After packaged-app validation exposed a first-run regression, restore the v0.4.1 contract that setup guidance remains available independently of license and publishing-data availability.

## Scope
- Update the Electron development dependency and lockfile to the supported Electron 44 line.
- Review Electron 41–44 breaking changes that affect the current application.
- Run focused runtime, launcher, UI, and packaging contract tests.
- Run broader release verification after focused checks pass.
- Record manual Windows and macOS packaged-app checks still required.
- Keep setup readiness independent from license status and Google Sheet-backed dashboard operations.
- Cover the packaged first-run condition where configuration and license context are both absent.

## Explicit Non-goals
- Do not disable GPU acceleration or add a GPU compatibility mode in this stage.
- Do not claim that the Electron upgrade resolves the unconfirmed Windows blank-window report.
- Do not add unrelated UI, feature, release publication, tag, or deployment work.

## Proposed Design
Keep Chromium's default hardware acceleration behavior unchanged. Upgrade only the Electron runtime and treat the Windows blank-window report as a separate diagnostic concern until it can be reproduced or supported by logs.

The application does not directly rely on WebGL or a dedicated GPU API. Chromium may still use GPU acceleration internally for compositing, CSS effects, and image rendering.

Setup readiness is derived from local configuration and cached Google OAuth state through one shared read model. Both system status and account overview use that model, while the Dashboard reads system setup status independently. A license lookup failure therefore affects subscription and usage presentation, but cannot hide the first-run setup guidance. Google Sheet-backed publishing operations are not requested until Google setup is complete.

## Affected Boundaries
- `package.json` and `package-lock.json`
- Electron main-process startup and BrowserWindow creation
- development and production launchers
- macOS and Windows packaging contracts
- optional Kuzu native-module loading and fallback
- setup readiness read model, account overview, system config status, and Dashboard loading behavior

## Decisions and Tradeoffs
- Upgrade during v0.4.3 preparation because the release is not urgent and can absorb compatibility testing.
- Keep GPU acceleration enabled by default; there is not enough evidence to apply a global workaround.
- Use a short-lived feature branch based on `release/v0.4.3` so the runtime change can be reviewed and merged independently.
- The user explicitly approved Electron 44 and macOS 13+ as the v0.4.3 compatibility baseline. Electron 43 is no longer the target.
- Preserve the simpler v0.4.1 first-run behavior: local setup guidance must not depend on a successful license request.
- Treat missing Google setup as a normal onboarding state rather than a publishing-dashboard error.

## Progress
- 2026-09-05: confirmed the current declared and locked Electron version is `40.6.1`.
- 2026-09-05: confirmed there is no explicit GPU-disable switch or direct WebGL/Canvas rendering dependency in the desktop UI.
- 2026-09-05: created this branch from `release/v0.4.3`, preserving the existing uncommitted release-preparation changes.
- 2026-09-05: an initial dependency installation attempted on the parent branch failed before modifying files because the sandbox could not resolve the npm registry. The operation will be retried on this branch with network approval.
- 2026-09-05: updated the declared and locked runtime to Electron `44.2.0` and downloaded the matching macOS arm64 binary.
- 2026-09-05: reviewed Electron 41–44 breaking changes. No directly used removed API was found. Electron 44 removes macOS 12 and Windows 32-bit support; the project already packages Windows x64, but choosing Electron 44 would raise the macOS minimum to 13.
- 2026-09-05: confirmed Electron 44 starts the main process, UI server, and BrowserWindow on macOS. The user visually confirmed that the UI appeared.
- 2026-09-05: confirmed the apparent server-feature outage in the direct `npm start` run came from the intentionally unselected runtime environment. The local UI root, CSS, and JavaScript assets all returned HTTP 200.
- 2026-09-05: added a release contract that keeps the desktop runtime on the selected Electron 44 major line.
- 2026-09-05: packaged macOS arm64 and Windows x64 artifacts successfully with Electron `44.2.0` using the release packaging options.
- 2026-09-05: verified the packaged macOS app reports version `0.4.3`, requires macOS `13.0`, includes the configured `.icns`, and contains the expected ASAR and default config resource.
- 2026-09-05: verified the packaged Windows executable is a 64-bit PE GUI binary and the artifact records Electron `44.2.0`.
- 2026-09-05: reproduced the packaged first-run regression with a sample-derived empty `config.json`. The production runtime and local UI server were healthy, but account overview failed while attempting to issue a one-time test license, which hid the new Dashboard onboarding guidance.
- 2026-09-05: compared v0.4.1 and v0.4.3 and identified the regression: v0.4.1 read onboarding from license-independent config status, while the productized Dashboard read it only from account overview.
- 2026-09-05: extracted a shared setup-readiness model, exposed it through config status, kept account overview available when license context is unavailable, and made the Dashboard load onboarding independently.
- 2026-09-05: changed the unconfigured Google state from a generic publishing failure to a setup-required presentation and skipped the Google Sheet-backed operations request until setup is complete.
- 2026-09-05: advanced the release checkpoint metadata to `v0.4.3-dev2` after the first-run guidance regression passed focused, browser, and full unit verification.

## Verification Plan
- Confirm the installed Electron version and lockfile integrity.
- Review the final dependency diff for unrelated upgrades.
- Run focused launcher and Electron/UI contract tests.
- Run relevant packaging contract tests for Windows and macOS.
- Exercise browser behavior with a failed account/license response and incomplete local setup.
- Run the full unit suite before parent merge unless the user explicitly narrows final verification.
- Manually verify packaged startup and visible UI on Windows and macOS before release approval.

## Current Risks
- Electron 44 changes the embedded Chromium and Node runtimes and may expose launcher or preload compatibility issues.
- `kuzu` is a native dependency and must continue to load or fall back safely in packaged environments.
- The original Windows blank-window cause remains unconfirmed and cannot be considered fixed solely by upgrading Electron.
- Electron 44 requires macOS 13 or later; this is now an explicitly approved v0.4.3 requirement and is documented for users.
- A rebuilt package still needs a manual empty-config launch check before the release candidate is approved.

## Verification
- Electron runtime probe: Electron `44.2.0`, Chromium `152.0.7977.76`, Node `24.20.0`.
- Focused launcher and packaging contracts: 25 passed.
- Browser UI smoke: passed with 191 fixture requests.
- Full unit suite after the first-run patch: 1,449 passed, 0 failed.
- Version/runtime contract: 3 passed.
- Manual macOS source run: UI appeared and the local UI server and static assets loaded successfully.
- Setup/account/system focused tests: 19 passed.
- Dashboard shell contracts: 10 passed.
- Browser UI smoke including account failure plus incomplete setup: passed with 207 fixture requests.
- Manual packaged Windows and macOS verification remains required before release approval.

## Current Handoff
Electron 44.2.0, the macOS 13+ compatibility baseline, and the packaged first-run setup regression patch are implemented and verified for `v0.4.3-dev2`. Rebuild the package and confirm that an empty configuration shows setup guidance before final release acceptance.
